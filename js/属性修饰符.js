 属性描述符概念及使用：
   属性描述概念：属性描述符用于描述属性的特点，如值、可重写性、可遍历性等。
   获取描述符：通过`Object.getOwnPropertyDescriptor`函数可获取对象属性的描述符，返回一个描述属性的对象。
   设置描述符：使用`Object.defineProperty`函数可重新定义属性的描述符，除设置属性值外，还可设置`writable`（可重写性）、`enumerable`（可遍历性）、`configurable`（描述符可修改性）等。

 访问器（getter和setter）：
   访问器概念：在属性描述符中配置`get`和`set`函数，分别作为读取器和设置器，整体称为访问器。
   读取和赋值操作：读取属性时运行`get`函数，赋值时运行`set`函数，属性不再是普通属性，而是两个函数在起作用。
   避免无限递归：不能在`get`和`set`函数中直接使用被定义的属性，否则会导致无限递归报错。
   只读属性实现：通过`get`函数返回固定值，在`set`函数中抛出错误，可实现只读属性，让使用者明确属性不可赋值。

 构造函数属性改造：
   data属性改造：使用属性描述符将`data`属性设置为不可重写、不可重新配置，通过`get`函数返回数据，在`set`函数中报错，确保属性只读。
   choose属性改造：使用`Object.defineProperty`设置`choose`属性，在`set`函数中进行类型判断，确保属性值为大于等于0的正整数，避免属性值混乱。
   总价属性改造：将总价属性改为通过`get`函数计算，使其看起来像属性，但本质是函数，避免数据冗余，且可使用ES6语法糖简化写法。
   
 其他考虑因素：
   原始数据改动：使用`Object.freeze`冻结克隆对象，避免原始数据被改动。
   对象属性添加：可使用`Object.freeze`冻结对象自身及原型，防止添加属性；使用`Object.seal`密封对象，可防止添加属性，但可修改现有属性。